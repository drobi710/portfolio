import React from 'react';
import Hero from './HeroComponent';

function Home(props) {
    return (
        <div className="container">
            <Hero />
        </div>
    );
}

export default Home;